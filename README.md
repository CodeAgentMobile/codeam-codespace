# codeam-codespace

Dev-environment template for CodeAgent Mobile cloud workspaces. The devcontainer
boots a prebaked image so a workspace is ready to use on first open instead of
installing tooling at start time.

Internal infrastructure — implementation notes live in the private engineering
docs, not here.
